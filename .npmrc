# Pin exact versions on install (no floating ^ / ~) so the manifest and lockfile agree.
save-exact=true

# Refuse to run if the local engine does not satisfy package.json "engines".
engine-strict=true
